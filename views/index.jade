extends layout

block content
  h1= title
  p Welcome to #{title}
  a(href="/signup") 注册

  #content
      .panel
          .header
              ul.breadcrumb
                  li
                    a(href='/') 主页
                    span.divider
                  li.active 登陆
          .inner
              if(error)
                  .alert.alert-danger
                      a.close(data-dismiss="alert",href="#") &times;
                      strong= error
              form#signin_form.form-horizontal(action='/signin',method='post')
                  .form-group
                      label.control-label.col-sm-2(for='name') 用户名
                      .col-sm-10
                          input#name.input-lg(type='text',name='name',size='30')
                  .form-group
                      label.control-label.col-sm-2(for='pass') 密码
                      .col-sm-10
                          input#pass.input-lg(type='password',name='pass',size='30')
                  input(type='hidden',name='_csrf',value='#{csrf || ""}')

                  .form-group
                      .col-sm-offset-2.col-sm-10
                          button.btn.btn-primary(type="submit") 登录
                          a(href="/auth/github")
                              span 通过Github 登陆
                          a#forgot_password(href='/search_pass') 忘记密码了？